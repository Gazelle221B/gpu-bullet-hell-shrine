[package]
name = "shared"
version = "0.2.0"
edition = "2021"
rust-version = "1.77"

[dependencies]
bytemuck = { version = "1.16", features = ["derive"] }
serde = { version = "1.0", features = ["derive"], optional = true }
